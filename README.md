# How do

```bash
pip install -r requirements.txt # or pip install monai[all] matplotlib

python main.py train -e 100 -d path/to/dataset.json

python main.py infer -d path/to/dataset.json

python visualize.py # Shows the result

```
